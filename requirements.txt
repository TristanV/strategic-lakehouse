# ───────────────────────────────────────────────
# Strategic Lakehouse — requirements.txt
# Tristan Vanrullen — 2026
# Environnement conda : strategic-lakehouse (Python 3.11)
#
# Installation :
#   conda create -n strategic-lakehouse python=3.11 -y
#   conda activate strategic-lakehouse
#   pip install --upgrade pip
#   pip install -r requirements.txt
#   pip install dbt-core dbt-duckdb   # installé séparément (résolution de dép.)
#   nbstripout --install              # hook git nettoyage notebooks
# ───────────────────────────────────────────────

# ── Ingénierie des données & transformation ───────────
# Manipulation de données tabulaires
pandas>=2.2.0
numpy>=1.26.0

# Moteur SQL analytique embarqué (pipeline Bronze → Silver → Gold)
duckdb>=1.0.0

# Format Parquet (lecture/écriture depuis pandas et duckdb)
pyarrow>=16.0.0

# Format Delta Lake (Phase 3 — activer si nécessaire)
# deltalake>=0.19.0

# ── Qualité des données ───────────────────────────
# Assertions et tests de qualité sur les données
great-expectations>=0.18.0

# Soda Core — règles déclaratives SQL/YAML, data contracts
soda-core>=3.3.0
soda-core-duckdb>=3.3.0

# ── API & exposition des KPI ──────────────────────
# Framework API REST asynchrone (couche Gold → API)
fastapi>=0.111.0
uvicorn[standard]>=0.30.0

# Sérialisation / validation des données (utilisé par FastAPI)
pydantic>=2.7.0

# ── Exploration & notebooks ───────────────────────
jupyterlab>=4.2.0
nbformat>=5.10.0

# Nettoyage des outputs de notebooks avant commit
nbstripout>=0.7.0

# ── Visualisation (EDA & diagnostics) ──────────────
matplotlib>=3.9.0
seaborn>=0.13.0
plotly>=5.22.0

# ── Lineage & métadonnées ──────────────────────
# Client OpenLineage (traçabilité du pipeline)
openlineage-python>=1.15.0

# ── Contrôle qualité du code ────────────────────
# Linter & formateur (remplace flake8+black+isort)
ruff>=0.4.0

# Typage statique
mypy>=1.10.0

# ── Tests ────────────────────────────────────
pytest>=8.2.0
pytest-cov>=5.0.0
httpx>=0.27.0          # client HTTP pour tester les endpoints FastAPI

# ── Utilitaires divers ─────────────────────────
# Variables d'environnement depuis fichier .env
python-dotenv>=1.0.0

# Typage avancé & compatibilité
typing-extensions>=4.11.0

# Journalisation structurée
loguru>=0.7.0

# ── CLI & automation (Phase 3 — optionnel) ────────
# Orchestration simple des tâches pipeline
# Décommenter si Prefect ou Airflow est retenu en Phase 3
# prefect>=3.0.0
# apache-airflow>=2.9.0

# ── Notes ─────────────────────────────────────
# dbt-core et dbt-duckdb sont installés séparément (voir ci-dessus).
# Pour exporter l'environnement conda complet :
#   conda env export > environment.yml
# Pour recréer à partir de l'export :
#   conda env create -f environment.yml
